# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..developers_commons.types.apps_filter import AppsFilter
from ..developers_commons.types.apps_response import AppsResponse
from ..developers_commons.types.marketplace_app_detail import MarketplaceAppDetail
from .raw_client import AsyncRawAppDirectoryClient, RawAppDirectoryClient
from .types.directory_app_sort_field import DirectoryAppSortField
from .types.get_directory_app_setting_download_url_response import GetDirectoryAppSettingDownloadUrlResponse
from .types.get_directory_app_setting_upload_url_response import GetDirectoryAppSettingUploadUrlResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AppDirectoryClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAppDirectoryClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAppDirectoryClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAppDirectoryClient
        """
        return self._raw_client

    def search(
        self,
        *,
        sort: typing.Optional[DirectoryAppSortField] = OMIT,
        filter: typing.Optional[AppsFilter] = OMIT,
        page: typing.Optional[int] = OMIT,
        size: typing.Optional[int] = OMIT,
        sort_desc: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AppsResponse:
        """
        Lists apps available to install on the agent, with filtering and pagination.

        Parameters
        ----------
        sort : typing.Optional[DirectoryAppSortField]
            The field to sort by. Defaults to the app's id.

        filter : typing.Optional[AppsFilter]
            Narrows the results. Omit to list everything available to the agent.

        page : typing.Optional[int]
            Page number to return, defaults to 0

        size : typing.Optional[int]
            The size of the page to return, defaults to 20. Max 1000.

        sort_desc : typing.Optional[bool]
            Whether to sort descending, defaults to true

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppsResponse

        Examples
        --------
        from mavenagi import MavenAGI

        client = MavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )
        client.app_directory.search()
        """
        _response = self._raw_client.search(
            sort=sort, filter=filter, page=page, size=size, sort_desc=sort_desc, request_options=request_options
        )
        return _response.data

    def get(self, app_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> MarketplaceAppDetail:
        """
        Gets an app and its installation status for the agent.

        Parameters
        ----------
        app_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MarketplaceAppDetail

        Examples
        --------
        from mavenagi import MavenAGI

        client = MavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )
        client.app_directory.get(
            app_id="appId",
        )
        """
        _response = self._raw_client.get(app_id, request_options=request_options)
        return _response.data

    def install(
        self,
        app_id: str,
        *,
        settings: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Installs the app on the agent, or updates settings for an existing installation. Re-runs the postInstall lifecycle hook on each call.

        Parameters
        ----------
        app_id : str

        settings : typing.Optional[typing.Any]
            The app's configuration, as an object keyed by setting key.

            The keys and accepted values are defined by the app itself: fetch the app with
            `get` and read its `settingsSchema`, where every entry carries a `key`, a `type`, and
            whether it is `required`. Values are merged into the installation's existing settings:
            keys you omit keep their stored value, and the server does not currently reject an unknown
            key or require an entry the schema marks `required`. Sending an empty object installs an
            app that declares no settings.

            Sensitive entries are returned redacted by `get`, and sending one back unchanged here is a
            no-op that keeps the stored value, so a settings object can be read, edited and written
            back whole. See `MarketplaceAppDetail.settings` for what is redacted and how.

            The value's JSON type follows the entry's `type`:

            | Entry type | Value |
            | --- | --- |
            | `text`, `multiline`, `color`, `dropdown` | string |
            | `number` | number |
            | `checkbox`, `switch` | boolean |
            | `array` | list of strings |
            | `complexarray` | list of objects |
            | `oneOf`, `jsonSchema` | object matching the entry's schema |
            | `image` | omitted — see below |

            `image` entries are file settings. Do not send their bytes here: upload them first with
            `getSettingUploadUrl` using the same setting key, and the stored file is picked up
            automatically. `section` entries are layout only and take no value.

            ```json
            {
              "settings": {
                "acme.api.key": "sk-live-...",
                "acme.region": "us-east-1",
                "acme.sync.enabled": true,
                "acme.retry.count": 3
              }
            }
            ```

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from mavenagi import MavenAGI

        client = MavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )
        client.app_directory.install(
            app_id="appId",
            settings={"key": "value"},
        )
        """
        _response = self._raw_client.install(app_id, settings=settings, request_options=request_options)
        return _response.data

    def uninstall(self, app_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Uninstalls the app from the agent.

        Parameters
        ----------
        app_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from mavenagi import MavenAGI

        client = MavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )
        client.app_directory.uninstall(
            app_id="appId",
        )
        """
        _response = self._raw_client.uninstall(app_id, request_options=request_options)
        return _response.data

    def get_setting_download_url(
        self, app_id: str, settings_key: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetDirectoryAppSettingDownloadUrlResponse:
        """
        Gets a url for downloading a file app setting.

        Parameters
        ----------
        app_id : str

        settings_key : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetDirectoryAppSettingDownloadUrlResponse

        Examples
        --------
        from mavenagi import MavenAGI

        client = MavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )
        client.app_directory.get_setting_download_url(
            app_id="appId",
            settings_key="settingsKey",
        )
        """
        _response = self._raw_client.get_setting_download_url(app_id, settings_key, request_options=request_options)
        return _response.data

    def get_setting_upload_url(
        self,
        app_id: str,
        settings_key: str,
        *,
        content_length: int,
        content_type: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetDirectoryAppSettingUploadUrlResponse:
        """
        Gets a presigned url for uploading a file app setting before installation.

        Parameters
        ----------
        app_id : str

        settings_key : str

        content_length : int

        content_type : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetDirectoryAppSettingUploadUrlResponse

        Examples
        --------
        from mavenagi import MavenAGI

        client = MavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )
        client.app_directory.get_setting_upload_url(
            app_id="appId",
            settings_key="settingsKey",
            content_length=1,
            content_type="contentType",
        )
        """
        _response = self._raw_client.get_setting_upload_url(
            app_id,
            settings_key,
            content_length=content_length,
            content_type=content_type,
            request_options=request_options,
        )
        return _response.data


class AsyncAppDirectoryClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAppDirectoryClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAppDirectoryClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAppDirectoryClient
        """
        return self._raw_client

    async def search(
        self,
        *,
        sort: typing.Optional[DirectoryAppSortField] = OMIT,
        filter: typing.Optional[AppsFilter] = OMIT,
        page: typing.Optional[int] = OMIT,
        size: typing.Optional[int] = OMIT,
        sort_desc: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AppsResponse:
        """
        Lists apps available to install on the agent, with filtering and pagination.

        Parameters
        ----------
        sort : typing.Optional[DirectoryAppSortField]
            The field to sort by. Defaults to the app's id.

        filter : typing.Optional[AppsFilter]
            Narrows the results. Omit to list everything available to the agent.

        page : typing.Optional[int]
            Page number to return, defaults to 0

        size : typing.Optional[int]
            The size of the page to return, defaults to 20. Max 1000.

        sort_desc : typing.Optional[bool]
            Whether to sort descending, defaults to true

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppsResponse

        Examples
        --------
        import asyncio

        from mavenagi import AsyncMavenAGI

        client = AsyncMavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )


        async def main() -> None:
            await client.app_directory.search()


        asyncio.run(main())
        """
        _response = await self._raw_client.search(
            sort=sort, filter=filter, page=page, size=size, sort_desc=sort_desc, request_options=request_options
        )
        return _response.data

    async def get(
        self, app_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MarketplaceAppDetail:
        """
        Gets an app and its installation status for the agent.

        Parameters
        ----------
        app_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MarketplaceAppDetail

        Examples
        --------
        import asyncio

        from mavenagi import AsyncMavenAGI

        client = AsyncMavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )


        async def main() -> None:
            await client.app_directory.get(
                app_id="appId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(app_id, request_options=request_options)
        return _response.data

    async def install(
        self,
        app_id: str,
        *,
        settings: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Installs the app on the agent, or updates settings for an existing installation. Re-runs the postInstall lifecycle hook on each call.

        Parameters
        ----------
        app_id : str

        settings : typing.Optional[typing.Any]
            The app's configuration, as an object keyed by setting key.

            The keys and accepted values are defined by the app itself: fetch the app with
            `get` and read its `settingsSchema`, where every entry carries a `key`, a `type`, and
            whether it is `required`. Values are merged into the installation's existing settings:
            keys you omit keep their stored value, and the server does not currently reject an unknown
            key or require an entry the schema marks `required`. Sending an empty object installs an
            app that declares no settings.

            Sensitive entries are returned redacted by `get`, and sending one back unchanged here is a
            no-op that keeps the stored value, so a settings object can be read, edited and written
            back whole. See `MarketplaceAppDetail.settings` for what is redacted and how.

            The value's JSON type follows the entry's `type`:

            | Entry type | Value |
            | --- | --- |
            | `text`, `multiline`, `color`, `dropdown` | string |
            | `number` | number |
            | `checkbox`, `switch` | boolean |
            | `array` | list of strings |
            | `complexarray` | list of objects |
            | `oneOf`, `jsonSchema` | object matching the entry's schema |
            | `image` | omitted — see below |

            `image` entries are file settings. Do not send their bytes here: upload them first with
            `getSettingUploadUrl` using the same setting key, and the stored file is picked up
            automatically. `section` entries are layout only and take no value.

            ```json
            {
              "settings": {
                "acme.api.key": "sk-live-...",
                "acme.region": "us-east-1",
                "acme.sync.enabled": true,
                "acme.retry.count": 3
              }
            }
            ```

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from mavenagi import AsyncMavenAGI

        client = AsyncMavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )


        async def main() -> None:
            await client.app_directory.install(
                app_id="appId",
                settings={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.install(app_id, settings=settings, request_options=request_options)
        return _response.data

    async def uninstall(self, app_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Uninstalls the app from the agent.

        Parameters
        ----------
        app_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from mavenagi import AsyncMavenAGI

        client = AsyncMavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )


        async def main() -> None:
            await client.app_directory.uninstall(
                app_id="appId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.uninstall(app_id, request_options=request_options)
        return _response.data

    async def get_setting_download_url(
        self, app_id: str, settings_key: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetDirectoryAppSettingDownloadUrlResponse:
        """
        Gets a url for downloading a file app setting.

        Parameters
        ----------
        app_id : str

        settings_key : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetDirectoryAppSettingDownloadUrlResponse

        Examples
        --------
        import asyncio

        from mavenagi import AsyncMavenAGI

        client = AsyncMavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )


        async def main() -> None:
            await client.app_directory.get_setting_download_url(
                app_id="appId",
                settings_key="settingsKey",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_setting_download_url(
            app_id, settings_key, request_options=request_options
        )
        return _response.data

    async def get_setting_upload_url(
        self,
        app_id: str,
        settings_key: str,
        *,
        content_length: int,
        content_type: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetDirectoryAppSettingUploadUrlResponse:
        """
        Gets a presigned url for uploading a file app setting before installation.

        Parameters
        ----------
        app_id : str

        settings_key : str

        content_length : int

        content_type : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetDirectoryAppSettingUploadUrlResponse

        Examples
        --------
        import asyncio

        from mavenagi import AsyncMavenAGI

        client = AsyncMavenAGI(
            organization_id="YOUR_ORGANIZATION_ID",
            agent_id="YOUR_AGENT_ID",
            app_id="YOUR_APP_ID",
            app_secret="YOUR_APP_SECRET",
        )


        async def main() -> None:
            await client.app_directory.get_setting_upload_url(
                app_id="appId",
                settings_key="settingsKey",
                content_length=1,
                content_type="contentType",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_setting_upload_url(
            app_id,
            settings_key,
            content_length=content_length,
            content_type=content_type,
            request_options=request_options,
        )
        return _response.data
