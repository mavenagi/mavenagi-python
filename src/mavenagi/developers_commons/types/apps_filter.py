# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ...core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ...core.serialization import FieldMetadata
from .app_capability import AppCapability
from .app_category import AppCategory
from .app_installed import AppInstalled
from .app_visibility import AppVisibility


class AppsFilter(UniversalBaseModel):
    """
    Narrows the apps returned by a directory search. Every field is optional; omitting all of them
    returns every app visible to the agent. Fields combine with AND, and list fields match any of
    their values.
    """

    search: typing.Optional[str] = pydantic.Field(default=None)
    """
    Case-insensitive substring match against an app's id, name, or description.
    """

    visibility: typing.Optional[typing.List[AppVisibility]] = pydantic.Field(default=None)
    """
    Only return apps with one of these visibilities.
    """

    installed: typing.Optional[AppInstalled] = pydantic.Field(default=None)
    """
    Only return apps that are installed on this agent, or only those that are not. Omit to
    return both.
    """

    category: typing.Optional[typing.List[AppCategory]] = pydantic.Field(default=None)
    """
    Only return apps tagged with one of these categories.
    """

    capability: typing.Optional[typing.List[AppCapability]] = pydantic.Field(default=None)
    """
    Only return apps offering one of these capabilities.
    """

    filter_count_enabled: typing_extensions.Annotated[
        typing.Optional[bool], FieldMetadata(alias="filterCountEnabled")
    ] = pydantic.Field(default=None)
    """
    Also compute `allAppsCountByFilter` and `installedAppsCountByFilter` on the response.
    These are the counts a filter sidebar needs, and computing them costs extra queries, so
    they are off by default.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
